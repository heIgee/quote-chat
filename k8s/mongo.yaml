apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quote-mongo
  template:
    metadata:
      labels:
        app: quote-mongo
    spec:
      containers:
        - name: mongo
          image: mongo:latest
          ports:
            - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: quote-mongo
spec:
  selector:
    app: quote-mongo
  ports:
    - port: 27017
      targetPort: 27017
